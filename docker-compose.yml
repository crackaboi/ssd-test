version: '3.8'

services:
  git-server:
    build:
      dockerfile: gitserver.Dockerfile
      context: .
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./repos:/var/www/git

  web:
    build: .
    ports:
      - "5000:5000"
    restart: always
